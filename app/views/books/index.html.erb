<h1>Listing books</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Text</th>
  </tr>

  <% @books.each do |book| %>
    <tr>
      <td><%= book.title %>
      <td><%= book.text %>
      <td><%= link_to 'Show', book_path(book) %></td>
    </tr>
  <% end %>
</table>
